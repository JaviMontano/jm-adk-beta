# Pristino Beta — Core Contract

Generated adapter (source: runtime/core.md + delta). Do NOT hand-edit — regenerated from delta on every build; manual changes are lost. [DOC]

## Identity

Catalog-driven harness for the **vibe coder** and the **AI-native knowledge worker**. Domain, brand, and commercial standards come from the **active profile** (`profiles/`), not the core — resolve the active profile FIRST, before any output. [DOC]
- Default profile: `profiles/metodologia.md`. Persona defaults: `profiles/vibe-coder.md`, `profiles/knowledge-worker.md`. [CONFIG]
- [SUPUESTO] A profile may declare one or more brands; if brand is ambiguous WITHIN the active profile, HALT and ask — never default across profiles.

## Hard rules

1. Evidence tags on every claim: `[CÓDIGO]` `[CONFIG]` `[DOC]` `[INFERENCIA]` `[SUPUESTO]`. Untagged claim = defect.
2. Estimation Integrity: estimates are COMPUTED — from task decomposition + deterministic scripts (`scripts/`) + cited sources, never from token-count or gut. Effort in explicit units; confidence + assumptions tagged. Currency/pricing is profile-scoped (the active profile may forbid client prices); core forbids only *uncomputed* estimates. [CONFIG]
3. Read before write; `catalog/skills.json` is the single source of truth. Stale read → re-read, never assume.
4. Script-first: any step expressible as a script IS a script under `scripts/`. Prose only where logic is non-deterministic.
5. Constitution v7.0.0 enforced in execution phases: extract MUST / MUST NOT, HALT on violation (`references/ontology/constitution-v7.0.0.md`). [DOC]
6. Verification before done — proven by artifact existence, never by assertion.

## Skill protocol

- Tier-0 index = one line per skill. Invoke → Read that skill's `SKILL.md` only; never preload siblings.
- Routers (®): resolve `params` from request (ask ONLY if ambiguous), Read exactly ONE playbook from `routes:`. Never load the whole cluster.
- `depth=quick|deep`; default `quick`. Escalate to `deep` only on explicit request or failed `quick` pass.
- Subagent output compressed (locator / receipt / findings, `references/roles/`).
- Auto-clarity override — use normal prose (not compressed) for: security warnings, irreversible actions, ordered sequences.

## Phase gates

- Completion = artifact existence: `scripts/check-prerequisites.sh --phase <p> --json`. Truth is the filesystem, not the log.
- Soft gates warn and continue; hard gates require 100% and BLOCK on miss.
- [SUPUESTO] `--json` output is machine-parsed by the orchestrator; non-zero exit = gate failure.

## Assumptions / limits (anti-scope)

- [SUPUESTO] This adapter governs runtime behavior only; build/regeneration of the adapter itself is out of scope here (owned by the delta + generator).
- Anti-scope: no cross-profile brand mixing, no uncomputed estimates, no untagged claims, no whole-cluster reads, no "done" without an artifact. Any of these = contract breach, HALT. (Price/currency emission is governed by the active profile, not forbidden here.)

## Acceptance criteria

- Every emitted claim carries exactly one evidence tag; every estimate is computed (decomposition/scripts/sources), never guessed.
- Active profile resolved before first content line; the profile's brand/currency rules respected (single brand per output within that profile).
- Each phase marked complete has its prerequisite artifacts on disk (verified via the gate script), not merely asserted.
- Constitution MUST/MUST NOT extracted and unviolated for every execution-phase action.

## Codex delta

Capabilities the base harness assumes but Codex lacks; each line = gap → workaround → check. [INFERENCE] from Codex runtime constraints.

- **Skill discovery**: no hooks, no native lookup. Skills table is inlined below; on invocation read `skills/<id>/SKILL.md` verbatim before acting. Anti-scope: don't fuzzy-match skill names — exact `<id>` only, else abort. Check: `ls skills/<id>/SKILL.md` resolves.
- **MCP config**: entries generated into `~/.codex/config.toml`. Env vars are NOT expanded there — never inline secrets; launch via `scripts/with-secrets.sh` wrapper. Edge case: missing wrapper or unset var → fail loud, do not run with empty credentials. Check: wrapper exits 0 and required vars are set.
- **Concurrency**: no subagent dispatch. Execute `[P]`-tagged tasks sequentially in listed order (parallelism is an optimization, not a correctness requirement). Trade-off: slower wall-clock, but deterministic and replayable. Acceptance: every `[P]` task ran exactly once, ordered, before declaring done.

## Active profile

This harness runs under the **metodologia** profile — its deliverable-quality, brand, i18n, and commercial standards apply. Load `profiles/metodologia.md` on demand; override with the `JMADK_PROFILE` env var. [CONFIG]

## Skills

`accessibility`® Accessibility router: WCAG audit, testing, accessible design, inclusive writing. Routes…
`adaptive-investigation-method` Investigar dominios desconocidos (repos, datasets, corpus) con mapeo barato, budget de…
`agent-orchestration`® Multi-agent orchestration: workflow execution, triad composition, routing, parallelism…
`agentic-loop-engineering` Construir el bucle de control agentico que enruta por stop_reason tipado con budget duro…
`ai-architecture`® AI/LLM system architecture: software arch, pipelines, conops, design patterns, audit…
`ai-quality`® AI quality: testing strategy, assisted testing, code review, safety, content detection…
`architecture`® Software/system architecture router: API design, DDD, events, realtime, caching…
`brand-output`® Branded output generation: HTML, DOCX, XLSX, folios, templates (MetodologIA DS tokens in…
`business-analysis`® Business analysis and change router: process modeling, requirements, feasibility…
`carrera`® Pack de carrera (es): proceso de seleccion, entrevistas, negociacion, CV, onboarding, red.
`claude-md-architecture` Estructurar memoria jerarquica CLAUDE.md user/team/module con at-imports y reglas…
`context-window-engineering` Disena el ensamblado de la ventana de contexto de un agente para maximizar reuso de…
`custom-tooling-extension` Decidir slash command vs skill y escribir su frontmatter de producción (context fork…
`daily-close` Cadencia de cierre diario: captura cerrado/pendiente/aprendido y siembra el dia siguiente…
`data-governance`® Router for data governance work: privacy patterns, data strategy, catalog/documentation…
`data-platform`® Data engineering lifecycle: pipelines, quality, validation, migration, export, flow…
`dbr-daily-plan` Cadencia DBR: selecciona <=3 prioridades-tambor del dia y emite el plan diario P09 con…
`devops-deploy`® CI/CD and release engineering router: pipelines, environments, deployment gates…
`docs-writing`® Documentation and professional writing router: technical docs, changelogs, diagrams…
`email-comms`® Email systems and communication: transactional sending, templates, and newsletters.
`evaluation-confidence-design` Disenar evaluacion con confidence calibrada contra labeled set, stratified sampling y…
`firebase`® Firebase platform router: auth, hosting, functions, firestore-adjacent setup, emulators…
`frontload-prompt` Reformatea un input largo o ambiguo a estructura SPEC…
`google-workspace`® Router for Google APIs/Workspace work — pick one topic and load its playbook: Sheets…
`guardrails`® Deterministic guard layer: pre/post tool guards, prompt filter, output contracts…
`hosting-infra`® Hosting and infrastructure: DNS, domains, SSL, CDN, serverless, backup/DR, and…
`iikit`® Intent Integrity Kit: spec-driven development pipeline…
`integrations`® Third-party service integration router: routes to one playbook for payment-integration…
`jarvis-bootstrap` Bootstrap del Jarvis OS: crea CLAUDE.md y MEMORY.md raiz y la estructura minima de…
`jarvis-os` Pack paraguas del Personal Jarvis OS: aplica COOL, detecta sector/estación, enruta a las…
`kata`® Agentic engineering katas: proven prompt/loop/tooling patterns from JM Labs.
`lab-session` Scaffold a JM Labs Lab session: the 4 canonical files (notas, hipotesis, referencias…
`legal-compliance`® Route legal/compliance work to one playbook: contract-review, compliance-assessment…
`market-intel`® Market and competitive intelligence: positioning, pricing, sector context, benchmarks…
`marketing-content`® Marketing content production: copywriting, calendars, PR, case studies, whitepapers…
`mcp-engineering` Configurar MCP servers (project vs user scope, env-var expansion) y disenar contratos de…
`message-batch-orchestration` Orquestar Message Batches API para cargas offline con custom_id unico, fragmentacion…
`monthly-audit` Auditoria mensual de salud del jarvis: rubrica de 6 preguntas (P22) que puntua, evidencia…
`observability`® Production health: monitoring, logging, alerting, health checks, and incident response.
`official-source-verifier` Verify a decision against official sources (ADK, Agent Skills spec, GitHub/Git…
`persistent-memory-design` Disena un scratchpad persistente en disco (Hypotheses/Decisions/Findings/Open) con solo…
`plan-mode-workflow` Disenar un gate de dos modos: explorar un repo desconocido en Plan Mode read-only, firmar…
`pm-delivery`® Project and delivery management router: budgets, estimation, capacity, roadmaps, OKRs…
`pristino-calibration` Read deterministic persona/mode/optimizer signals injected by persona-calibrate.sh and…
`product-analytics`® Product and business analytics router: event instrumentation, KPIs, A/B tests, cohorts…
`project-create` Scaffold a Jarvis OS project P-NNN-slug under 02_Proyectos/ with compliant CLAUDE.md…
`prompt-chaining-design` Descomponer tareas grandes en pase local tipado y pase de integracion sobre resumenes…
`prompting-and-meta-prompting` Turn vague intentions into durable, eval-ready prompts, meta-prompts, acceptance…
`provenance-engineering` Disenar pipelines de extraccion/sintesis con provenance tipada: invariante no hay claim…
`qbr-quarterly` Cadencia QBR (P13): audita el trimestre cerrado contra OKRs y planifica el proximo con…
`revisor-veracidad` Audita un texto, etiqueta cada afirmacion no reproducible con un tag de veracidad y…
`runtime-routing` Route agentic work across Claude, Codex, Gemini, Antigravity, VS Code, and local…
`safe-scripting-and-bash` Design and review safe, portable, dry-run-first Bash scripts for local agentic workflows…
`sales-bizdev`® Router for sales and business development in an ES/EN consulting context — prospecting…
`security`® Router for application security playbooks: auth, RBAC, input sanitization, headers/CORS…
`self-correction-loops` Construir verificacion cruzada declarado-vs-calculado con mismatch flag tipado y escalada…
`seo-growth`® SEO and conversion growth router: technical SEO, content SEO, landing pages, funnels…
`session-lifecycle-management` Decidir resume vs fork vs fresh con summary tipado segun validez de contexto y deteccion…
`session-workspace`® Router for agent session lifecycle: bootstrap, protocol, state/context-window management…
`skill-foundry`® Router for building/certifying agentic assets — skills, agents, commands, prompts, hooks…
`station-create` Scaffold a Jarvis OS station (universal or dedicated) with its own CLAUDE.md and…
`structured-output-design` Disenar extraccion estructurada Claude como contrato de datos: JSON Schema defensivo…
`subagent-orchestration` Design deterministic hub-and-spoke subagent orchestration plans with AgentDefinition plus…
`task-subfolder` Crea/retoma una sub-tarea T-NNN multi-sesion con CLAUDE.md, task.md y log.md (P33)…
`testing-qa`® Router for software testing strategy and execution: pick one topic — bdd-full-spectrum…
`tool-use-design` Design deterministic tool-description routing contracts with explicit input formats…
`ux-design`® UI/UX design patterns: design systems, interaction and motion, onboarding, microcopy, and…
`ux-research`® User research and validation router: route to interviews, surveys, usability testing, or…
`validation-retry-design` Design deterministic extract-validate-retry loops with actionable validation errors…
`wbr-weekly-review` Cadencia WBR (P11): repaso semanal de avances, estancado y friccion; produce un acta…
`web-frontend`® Frontend implementation router: pick ONE topic (react/angular/web-components…
`weekly-retro` Cadencia de retro semanal (P12): captura que ayudo, que friccion hubo, y promueve…
`workspace-setup` Design a deterministic, dry-run-first local workspace profile plan for .jm-adk.local.json…
